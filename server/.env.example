PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:5173
MONGO_URI=mongodb://127.0.0.1:27017/fixit_db
JWT_SECRET=fixit_super_secure_jwt_secret_key_2026_production_ready
JWT_EXPIRES_IN=7d

# Cloudinary Configuration (Optional - falls back to local uploads if empty)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Email / Nodemailer Configuration (Optional - falls back to Ethereal/Logger if empty)
EMAIL_HOST=smtp.ethereal.email
EMAIL_PORT=587
EMAIL_USER=
EMAIL_PASSWORD=
EMAIL_FROM=FixIt Platform <support@fixit.org>
